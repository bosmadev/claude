[project]
name = "claude-youtube-transcriber"
version = "1.0.0"
description = "YouTube transcription for Claude Code CLI"
requires-python = ">=3.13"
dependencies = [
    "youtube-transcript-api>=0.6.2",
    "yt-dlp>=2024.1.0",
]
